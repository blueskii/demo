<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.BoardDao">
	<insert id="insert" parameterType="com.example.demo.entity.BoardPost"
			useGeneratedKeys="true" keyProperty="id">
		INSERT INTO board_post (title, content, author)
		VALUES (#{title}, #{content}, #{author})
	</insert>

	<select id="findAll" resultType="com.example.demo.entity.BoardPost">
		SELECT id, title, content, author, created_at, updated_at
		FROM board_post
		ORDER BY id DESC
	</select>

	<select id="findById" parameterType="long" resultType="com.example.demo.entity.BoardPost">
		SELECT id, title, content, author, created_at, updated_at
		FROM board_post
		WHERE id = #{id}
	</select>

	<select id="existsByTitleExcludingId" resultType="boolean">
		SELECT CASE WHEN COUNT(*) > 0 THEN TRUE ELSE FALSE END
		FROM board_post
		WHERE title = #{title}
		<if test="id != null">
			AND id != #{id}
		</if>
	</select>

	<update id="update" parameterType="com.example.demo.entity.BoardPost">
		UPDATE board_post
		SET title = #{title},
			content = #{content},
			author = #{author},
			updated_at = CURRENT_TIMESTAMP
		WHERE id = #{id}
	</update>

	<delete id="deleteById" parameterType="long">
		DELETE FROM board_post WHERE id = #{id}
	</delete>
</mapper>
